{% trans_default_domain 'messages' %}

{% extends 'CharityBundle::layout_editor.html.twig' %}

{% block title %}{{ parent() }} - {{ "charity.content.donation.create"|trans|capitalize }} {% endblock %}

{% block breadcrumbmodule %}
    {{ parent() }}
    <a href=" {{ url('admin_charity_donation_list') }} ">
       {{ 'charity.content.donation.list'|trans|capitalize }}
    </a>
    <i class="material-icons">keyboard_arrow_right</i>
    <em>{{ "charity.content.donation.create"|trans|capitalize }}</em>
{% endblock %}

{% block content %}
    <div id="page_content_inner">
        {% include 'CharityBundle:Donation:form.html.twig' with { 'form' : form } %}
    </div>
{% endblock %}

{# {% block javascripts %}
    {{ parent() }}
    {% javascripts 
        'bundles/puzzleadmin/js/forms_advanced.min.js'
        output='compiled/js/admin_charity_donation_create.min.js'
    %}
    {% endjavascripts %}
    <script type="text/javascript">

        // Add donation line form
        $("#add-donation-line").click(function(e){
            e.preventDefault();
            var count = $("#count_form").val();
            count++;
            $("#count_form").val(count);
            $.get($('#add-new-donation-line-form').val() +'?count=' + count, function(data){
                $("#listProducts").append(data);
            })
        });

        // Remove donation line form
        $(".remove_line").click(function(e){
            e.preventDefault();
            $("#container_donation_line_" + $(this).attr('id')).fadeOut("fast");
            
        });
    </script>

{% endblock %} #}
