<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <title>Matrice</title>
    </head>
    <body style="margin:0px; background: #e8ebf0;">
        <div width="100%" style="background: #e8ebf0; padding: 0px 0px; font-family:arial; line-height:28px; height:100%;  width: 100%; color: #333;">
            <div style="max-width: 700px; padding: 50px 0; margin: 0px auto; font-size: 14px">
                <div style="padding: 30px; background: #fff;">
                    <table border="0" cellpadding="0" cellspacing="0" style="width: 100%;">
                        <tbody>
                            <tr>
                                <td>
                                    <h1 style="font-size: 20px;">{{ user.firstName }} {{ user.lastName }}</h1>
                                    <p>
                                        Votre compte a été 
                                        <b>
                                            {% if user.isActive == 1 %}
                                                réactivé
                                            {% else %}
                                                désactivé
                                            {% endif %}
                                        </b>
                                    </p>
                                    {% if user.isActive == 1 %}
                                        <p> 
                                            <a href="{{ url('homepage')}}" style="display: inline-block; padding: 10px 30px; font-size: 14px; color: #fff; background: #f44236; border-radius: 40px; text-decoration:none;">Accéder à votre espace</a>
                                        </p>
                                    {% endif %}

                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
               
            </div>
        </div>
    </body>
</html>